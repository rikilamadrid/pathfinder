# `<title>`

## The problem

`<what was wrong, concretely. No preamble, no context-setting paragraph.>`

## What I tried

`<the approach that did not survive, and what it cost. Drop this heading if
nothing was tried and abandoned — do not manufacture a struggle.>`

## What actually worked

`<the approach that shipped>`

## How it works

`<enough mechanism that a reader could rebuild the idea in their own stack>`

## An interesting technical detail

`<the specific thing that proves the author built this>`

## What I learned

`<what the work revealed that was not obvious when it started>`

## What this unlocks next

`<what is now possible. Not a summary of the article.>`
