1/ `<The actual claim. Not a tease, not "a thread 🧵".>`

2/ `<The problem, concretely.>`

3/ `<What was tried and did not work.>`

4/ `<What worked, and the mechanism in one post.>`

5/ `<The number, with its condition.>`

6/ `<What it unlocks, and the link.>`

`<One idea per post. Same evidence rules as the article — a short post is not a
place where an unsupported claim becomes acceptable.>`
