# Evidence

Scope: `<scope description>`
Range: `<resolved range, or "unresolved">`
Collected: `<counts: commits, files, PRs, tests, docs>`

Every entry carries at least one source reference. An entry that cannot be
sourced belongs nowhere in this file.

## Problem

- `<what was wrong or missing>` — `pr:NN`, `issue:NN`

## Previous state

- `<how it behaved before>` — `diff:path`, `doc:path`

## Changes made

- `<what now exists>` — `commit:sha`, `diff:path`

## Technical decisions

- **`<decision>`** over `<rejected alternative>` — `<reason>` — `doc:path`, `pr:NN`

## Failures and dead ends

- `<what did not work, and what it cost>` — `commit:sha`

## Measurable outcomes

| Measure | Value | Source |
| --- | --- | --- |
| `<what>` | `<number with unit>` | `test:path` |

## Lessons

- `<what the work revealed>` — `<the evidence that revealed it>`

## Next steps

- `<stated follow-up>` — `changelog:[Unreleased]`, `issue:NN`

## Assets

- `<path>` — `diff:path`

## Gaps

Reported by collection, plus anything looked for and not found. Not to be
resolved by reasoning.

- `<gap>`
