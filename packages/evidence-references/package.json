{
  "name": "@pathfinder/evidence-references-tests",
  "version": "0.0.0",
  "private": true,
  "description": "Repository-only tests and the shared conformance fixture for lib/evidence-references.mjs. Never installed into a project; never published.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test"
  }
}
